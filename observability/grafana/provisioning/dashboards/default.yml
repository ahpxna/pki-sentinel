apiVersion: 1
providers:
  - name: pki-sentinel
    orgId: 1
    folder: "pki-sentinel"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
